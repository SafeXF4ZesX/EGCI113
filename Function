#include <stdio.h>
#include <stdlib.h>
 void square (int);
 //1. Function Prototype
int main()
{
    int a=10;
    square(a); //2.Function call
    square(a+2);
    square(4);
    printf("a=%d",a);
    return 0;
}
//3 Function Implementation
void square(int x)
{
    x*=x;
    printf("%d\n",x);
}
