#include <stdio.h>
#include <stdlib.h>
#include <math.h>
void print_menu();
long fact(int);
int nCr(int,int);
int sum(int);

int main()
{
    int menu;
    int n,r;
    do{
        print_menu();
        scanf("%d",&menu);
        if(menu>=1 && menu<4){
            printf("Input your number ");
            scanf("%d",&n);
        }
    }
}

void print_menu(){
        print("Your choice?\n");
}

long fact(int,n){
    long  f;
    for()
    return f;
}
